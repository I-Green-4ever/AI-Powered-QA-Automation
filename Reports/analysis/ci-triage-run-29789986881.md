# CI Failure Triage — Run 29789986881

**Run:** https://github.com/I-Green-4ever/AI-Powered-QA-Automation/actions/runs/29789986881  
**Job:** https://github.com/I-Green-4ever/AI-Powered-QA-Automation/actions/runs/29789986881/job/88509465756  
**Commit:** `test(DS-3): add focused name validation coverage` (`59cb633c98bea137d0d18ca546cb477aa8ceec7d`)  
**Branch:** `cursor/ds-ticket-workflow-automation-1754` (push; open PR #2)  
**Result:** 8 failed · 69 passed · 12 skipped (89 total)  
**HTML report:** `Reports/playwright-report-29789986881/`  
**Artifact:** downloaded successfully; 53,007,512 bytes, eight retry traces present, retention expires 2026-08-20  
**Overall classification:** 4 test issues · 4 application bugs · 0 infrastructure/mocking failures

The repository contains the Playwright suite and POMs, but not the Didaxis application source. Application-handler locations below therefore identify the affected behavior; exact app source files/functions cannot be named from this repository.

## PR causality

At the failed SHA, the PR changed only:

- `features/DS-3.feature-validation.md`
- `features/DS-3.feature.md`
- `tests/ds3-validation.spec.ts`

Seven of the eight terminal failures are in files unchanged from base commit `b8cd7f8`; six of those fail identically in base-branch run [29755637606](https://github.com/I-Green-4ever/AI-Powered-QA-Automation/actions/runs/29755637606). DS-1 TC-007 did not fail in that baseline run, but its source is also unchanged and its result depends on residual rows in the shared tenant.

Only **DS-3 TC-006** is introduced by this PR. The PR therefore causes **1/8** terminal failures. The base branch was already red, so the other seven failures are not regressions from this PR.

## Summary by story

| Story | Total Failed | Test Errors | Locator Issues | App Bugs | Infra/Mocking | Caused by PR |
|---|---:|---:|---:|---:|---:|---:|
| DS-1 | 3 | 1 | 1 | 1 | 0 | 0 |
| DS-2 | 2 | 0 | 1 | 1 | 0 | 0 |
| DS-3 | 1 | 1 | 0 | 0 | 0 | 1 |
| DS-4 | 1 | 0 | 0 | 1 | 0 | 0 |
| DS-5 | 1 | 0 | 0 | 1 | 0 | 0 |
| **Total** | **8** | **2** | **2** | **4** | **0** | **1** |

## Per-failure diagnosis

### DS-1 — Create Program

#### TC-005 — Description is optional

| Field | Detail |
|---|---|
| Affected location | `tests/ds1-create-program.spec.ts:111` at run SHA |
| Classification | **Test code error** |
| Root cause | Invalid assertion chain: `expect(...).not.not.rejects.toBeVisible()`. Playwright locator assertions have no `rejects` modifier, so the chain dereferences `undefined`. |
| Expected | Created program row is visible. |
| Actual | `TypeError: Cannot read properties of undefined (reading 'rejects')` on the initial attempt and both retries. |
| Suggested fix | Use `await expect(programs.row(programName)).toBeVisible();`. |
| PR causality | No. The same line and failure are present in base run 29755637606. |
| Evidence | `data/e25f027efc3aa7a232c40d8aed7e1addf70af606.md`; retry trace `data/0899a2efdb1d1dae4bb5a9e87786864d10bb3ac3.zip`. |

#### TC-007 — Whitespace-only name

| Field | Detail |
|---|---|
| Affected locations | `tests/ds1-create-program.spec.ts:139`; `pages/components/program-row.ts:12-18` |
| Classification | **Test assertion / locator issue** |
| Root cause | `programRow('   ').editButton` builds the accessible-name locator `Edit    `. Role-name matching normalizes whitespace and is non-exact by default, so it matches every Edit button in the page instead of a program literally named three spaces. |
| Expected | Locator count 0. |
| Actual | Counts 6, 7, and 6 across attempts. The preceding `createButton.toBeDisabled()` assertion passed, so there is no evidence that the app submitted the whitespace-only value. |
| Suggested fix | Assert disabled state plus zero observed `POST /api/programs` requests. Do not use whitespace-only accessible text to identify a row. |
| PR causality | No. Source is unchanged at the run SHA. It surfaced because the shared tenant contained residual rows. |
| Evidence | `data/d85e027706af312f1bc39055cabaa8261530c5f5.md`; retry trace `data/4ba74ce09de96bb231811262bfc67038614e8b9e.zip`. |

#### TC-016 — Rapid double-click Create

| Field | Detail |
|---|---|
| Affected location | `tests/ds1-create-program.spec.ts:292-316`; application Create submit handler (app source unavailable) |
| Classification | **Application bug** |
| Root cause | Create is not protected by a synchronous single-flight lock and the backend does not reject the duplicate request. |
| Expected | One POST and one row. |
| Actual | Trace records two `POST /api/programs` requests, both returning 201, and the snapshot contains two rows with the same generated name. |
| Suggested fix | Disable/lock Create before dispatch, deduplicate in-flight submissions, and enforce server-side uniqueness/idempotency. |
| PR causality | No. Identical failure occurs in base run 29755637606. |
| Evidence | `data/25bca57c8ee65ea3d4dd1203334db4440664edc2.md`; trace `data/8dbcc6035bba3b95b2aadabc7851a836f4accbd6.zip`. |

### DS-2 — Edit Program

#### TC-010 — Whitespace-only name

| Field | Detail |
|---|---|
| Affected locations | `tests/ds2-edit-program.spec.ts:212`; `pages/programs.page.ts:55-57` |
| Classification | **Test assertion / locator issue** |
| Root cause | Page-wide `getByText('   ', { exact: true })` still applies Playwright whitespace normalization and resolves unrelated empty/whitespace nodes. |
| Expected | Locator count 0. |
| Actual | Locator count 121 on all three attempts. The preceding `saveButton.toBeDisabled()` assertion passed. |
| Suggested fix | Verify Save remains disabled and no PATCH occurs, or verify the original row remains. Do not assert page-wide whitespace text. |
| PR causality | No. Identical failure occurs in base run 29755637606. |
| Evidence | `data/98d9ccc46396357a28395cf1cbe61f5e7ae1586d.md`; trace `data/79a6d6f3e4acf98110c6db41afea4f936b57e906.zip`. |

#### TC-021 — Rapid double-click Save

| Field | Detail |
|---|---|
| Affected location | `tests/ds2-edit-program.spec.ts:389-418`; application Save submit handler (app source unavailable) |
| Classification | **Application bug** |
| Root cause | Save accepts a second activation while the first PATCH is in flight. |
| Expected | `patchCount === 1`. |
| Actual | `patchCount === 2` on all attempts. The retry trace records two PATCH requests to the same program ID, both returning 200. |
| Suggested fix | Add a synchronous in-flight guard/disabled state and make updates idempotent server-side. |
| PR causality | No. Identical failure occurs in base run 29755637606. |
| Evidence | `data/81b2c28259d4259369456194229ee03f2ecdb0f2.md`; trace `data/185c8b799d6992fbdf06396f723aa6cb4d2a0e7d.zip`. |

### DS-3 — Validation and duplicate prevention

#### TC-006 — Preserve values after duplicate rejection

| Field | Detail |
|---|---|
| Affected location | `tests/ds3-validation.spec.ts:128-148`, especially `test.fail` at line 139 |
| Classification | **Test code / synchronization issue** |
| Root cause | The test marks the entire case as expected to fail, but after `modal.clickCreate()` it only checks the transient open dialog and current field values. It does not wait for the duplicate POST outcome, require an error, or verify one row remains. Those assertions pass before the successful duplicate creation is observed, so Playwright reports `Expected to fail, but passed.` |
| Expected | Under the DS-153 annotation, at least one post-submit assertion should fail until duplicate creation is fixed. |
| Actual | All three attempts passed their assertions. The trace nevertheless records the original POST and duplicate POST both returning 201, confirming DS-153 is still present. |
| Suggested fix | Wait for the duplicate POST response or settled UI state, then assert the dialog/error/value state and `programs.row(programName).toHaveCount(1)`. Keep expected-failure scope tied to DS-153 until the app fix lands. |
| Cleanup impact | Because the test ends immediately while the response auto-tracker is asynchronous, the successful duplicate can escape per-test cleanup; the trace/snapshots show residual rows. Waiting for the settled result also gives the tracker time to register the created ID. |
| PR causality | **Yes.** This test and annotation were introduced at the failed PR SHA. |
| Evidence | Report message `Expected to fail, but passed.`; trace `data/ba5d5ab982de37ed68182a0c8960cfbd564c8051.zip` shows two POST 201 responses. |

DS-3 TC-005 is not one of the eight terminal failures because its `test.fail` annotation behaved as expected. It provides corroborating evidence for known bug **DS-153**: its error context shows no duplicate error and two rows with the exact same name.

### DS-4 — Delete Program

#### TC-019 — Rapid double-activation Delete

| Field | Detail |
|---|---|
| Affected location | `tests/ds4-delete-program.spec.ts:319-346`; application Delete handler (app source unavailable) |
| Classification | **Application bug** |
| Root cause | Both activations open confirmation dialogs and dispatch DELETE requests; there is no single-flight guard. |
| Expected | One dialog and one DELETE. |
| Actual | `dialogCount === 2`. Trace records two DELETEs for the same ID: first 200, second 404. |
| Suggested fix | Lock the delete action before opening confirmation/submitting and treat repeated server deletes idempotently. |
| PR causality | No. Identical failure occurs in base run 29755637606. |
| Evidence | `data/5638b5c3a0730ef4ea836162964ed71419ce45a0.md`; trace `data/582b959f9b969686911724e408a71f5a5063f6f2.zip`. |

### DS-5 — Program list

#### TC-007 — 5xx list load

| Field | Detail |
|---|---|
| Affected locations | `tests/ds5-list-filter.spec.ts:231-255`; `pages/programs.page.ts:27-32`; application list error-state handler (app source unavailable) |
| Classification | **Application bug** |
| Root cause | The list request error is mapped to the normal empty-data UI instead of an error state. |
| Expected | No data rows, no empty-state message, and an error indicator. |
| Actual | Mocked GET returned 500, but the snapshot displayed `No programs yet. Create your first program to get started.` and no rows. |
| Mock validation | The trace confirms the route produced `GET /api/programs → 500`; this is not a mocking failure. |
| Suggested fix | Preserve request error state separately from an empty successful response and render retry/error UX. |
| PR causality | No. Identical failure occurs in base run 29755637606. |
| Evidence | `data/13192a869d8b9c7c600ef3287fe749c97f9c62d6.md`; trace `data/32bd4221e0d77732baccd4df04786a62dc798da1.zip`. |

## Proposed patches — human review only

No source files were changed.

1. **PR-blocking DS-3 test fix:** synchronize TC-006 with the POST/settled UI and assert duplicate error plus one remaining row. This makes the DS-153 expected failure deterministic and prevents the false pass.
2. **DS-1 TC-005:** replace the invalid matcher chain with a normal visible-row assertion.
3. **Whitespace tests:** replace text locators for whitespace with disabled-button and zero-request assertions.
4. **Application actions:** add frontend single-flight guards and backend idempotency/uniqueness for Create, Save, and Delete.
5. **List error handling:** distinguish GET failure from an empty 200 response.

## Jira candidates

No Jira actions were taken.

- **DS-153 (existing):** exact duplicate creation. Confirmed by DS-3 TC-005 context and TC-006 trace; do not create a duplicate ticket.
- Candidate linked to **DS-1:** rapid Create sends two successful POSTs.
- Candidate linked to **DS-2:** rapid Save sends two successful PATCHes.
- Candidate linked to **DS-4:** rapid Delete sends two DELETEs and opens two dialogs.
- Candidate linked to **DS-5:** a list 500 renders the empty state instead of error UX.

## Priority recommendations

1. Fix DS-3 TC-006 synchronization/coverage before rerunning PR #2; it is the only terminal failure introduced by this PR.
2. Prioritize DS-153/backend uniqueness because it creates persistent duplicate data and contributes to shared-tenant pollution.
3. Add single-flight guards to Create, Save, and Delete.
4. Correct the two whitespace locator assertions and the DS-1 invalid matcher.
5. Correct DS-5 error-state rendering.

## Related artifacts

- Local HTML report: `Reports/playwright-report-29789986881/index.html`
- Trace files: `Reports/playwright-report-29789986881/data/*.zip`
- Full diagnosis: `Reports/analysis/ci-triage-run-29789986881.md`
- Failed logs: `gh run view 29789986881 --log-failed`
- Base comparison: `gh run view 29755637606 --log-failed`
